#pragma strict

/*
function OnCollisionEnter (col : Collision2D)
{
    if(col.gameObject.GetComponent(projectilePrefab).projectileColor == col.gameObject.GetComponent(colorController).playerColor)
    {
        Destroy(col.gameObject);
    }
}
*/

function Start () {

}

function Update () {



}